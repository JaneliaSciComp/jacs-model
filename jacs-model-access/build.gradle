project(":jacs-model-access") {
    dependencies {
        compile project(":jacs-model-core"),
                JACKSON_DATABIND_LIB,
                JONGO_LIB,
                MONGODB_DRIVER_LIB,
                SOLR_SEARCH_LIB

        compile (HIBERNATE_LIB) {
            exclude group: 'javax.transaction'
        }

        testCompile JACKSON_DATATYPE_JODA_LIB
    }
}
